var React = require('react'),
    Minesweeper = require('../game.js');

var Board = React.createClass({
  render: function() {
    return (
      <div>Im a real board!</div>
    );
  }
});

module.exports = Board;
